{
  "scripts": {
    "build": "cd audioleader-site && npm run build",
    "postinstall": "cd audioleader-site && npm install"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "jsdom": "^27.0.0"
  }
}
